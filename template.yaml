AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: >
  salmak

  Sample SAM Template for salmak

# More info about Globals: https://github.com/awslabs/serverless-application-model/blob/master/docs/globals.rst
Globals:
  Function:
    Timeout: 20
    MemorySize: 512

Resources:
  SalmakFunction:
    Type: AWS::Serverless::Function # More info about Function Resource: https://github.com/awslabs/serverless-application-model/blob/master/versions/2016-10-31.md#awsserverlessfunction
    Properties:
      CodeUri: SalmakFunction
      Handler: com.salmak.App::handleRequest
      Runtime: java21
      Architectures:
        - x86_64
      MemorySize: 512
      Environment: # More info about Env Vars: https://github.com/awslabs/serverless-application-model/blob/master/versions/2016-10-31.md#environment-object
        Variables:
          PARAM1: VALUE
          TABLE_NAME: Salmak-Users
      Policies:
        - DynamoDBCrudPolicy:
            TableName: Salmak-Users
      Events:
        RegisterUser:
          Type: Api
          Properties:
            Path: /register
            Method: post

Outputs:
  # ServerlessRestApi is an implicit API created out of Events key under Serverless::Function
  # Find out more about other implicit resources you can reference within SAM
  # https://github.com/awslabs/serverless-application-model/blob/master/docs/internals/generated_resources.rst#api
  SalmakApi:
    Description: "API Gateway endpoint URL for Prod stage for Salmak function"
    Value: !Sub "https://${ServerlessRestApi}.execute-api.${AWS::Region}.${AWS::URLSuffix}/Prod/register/"
  SalmakFunction:
    Description: "Salmak Lambda Function ARN"
    Value: !GetAtt SalmakFunction.Arn
  SalmakFunctionIamRole:
    Description: "Implicit IAM Role created for Salmak function"
    Value: !GetAtt SalmakFunctionRole.Arn
